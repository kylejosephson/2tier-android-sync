plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("org.jetbrains.kotlin.plugin.compose")
    id("org.jetbrains.kotlin.plugin.serialization")
}

android {
    namespace = "com.example.kylesmusicplayerandroid"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.example.kylesmusicplayerandroid"
        minSdk = 24
        targetSdk = 34

        // 🔼 VERSION BUMP
        versionCode = 2
        versionName = "1.1"
    }

    // ✅ RELEASE SIGNING (reads from ~/.gradle/gradle.properties)
    signingConfigs {
        create("release") {
            val storeFilePath = providers.gradleProperty("KMP_STORE_FILE").orNull
            val storePass = providers.gradleProperty("KMP_STORE_PASSWORD").orNull
            val keyAliasName = providers.gradleProperty("KMP_KEY_ALIAS").orNull
            val keyPass = providers.gradleProperty("KMP_KEY_PASSWORD").orNull

            if (storeFilePath != null) {
                storeFile = file(storeFilePath)
            }
            if (storePass != null) {
                storePassword = storePass
            }
            if (keyAliasName != null) {
                keyAlias = keyAliasName
            }
            if (keyPass != null) {
                keyPassword = keyPass
            }
        }
    }

    buildTypes {
        getByName("debug") {
            // unchanged
        }

        getByName("release") {
            signingConfig = signingConfigs.getByName("release")
            isMinifyEnabled = false
            isShrinkResources = false
        }
    }

    buildFeatures {
        compose = true
    }

    composeOptions {
        kotlinCompilerExtensionVersion = "1.5.14"
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }
}

dependencies {

    // ----------------------------------------
    // Jetpack Compose
    // ----------------------------------------
    val composeBom = platform("androidx.compose:compose-bom:2024.10.00")
    implementation(composeBom)

    implementation("androidx.compose.ui:ui")
    implementation("androidx.compose.ui:ui-tooling")
    implementation("androidx.compose.ui:ui-tooling-preview")
    implementation("androidx.compose.material3:material3")
    implementation("androidx.activity:activity-compose:1.9.1")
    implementation("androidx.lifecycle:lifecycle-viewmodel-compose:2.8.3")
    implementation("androidx.lifecycle:lifecycle-viewmodel-ktx:2.8.3")

    // ----------------------------------------
    // ExoPlayer / Media3
    // ----------------------------------------
    val media3Version = "1.3.1"
    implementation("androidx.media3:media3-exoplayer:$media3Version")
    implementation("androidx.media3:media3-common:$media3Version")
    implementation("androidx.media3:media3-session:$media3Version")
    implementation("androidx.media3:media3-ui:$media3Version")

    // ----------------------------------------
    // Networking (OneDrive REST)
    // ----------------------------------------
    implementation("com.squareup.okhttp3:okhttp:4.11.0")

    // ----------------------------------------
    // Storage Access Framework (SD card)
    // ----------------------------------------
    implementation("androidx.documentfile:documentfile:1.0.1")

    // ----------------------------------------
    // Icons
    // ----------------------------------------
    implementation("androidx.compose.material:material-icons-extended")

    // ----------------------------------------
    // Image loading
    // ----------------------------------------
    implementation("io.coil-kt:coil-compose:2.7.0")

    // ----------------------------------------
    // Kotlinx Serialization
    // ----------------------------------------
    implementation("org.jetbrains.kotlinx:kotlinx-serialization-json:1.6.3")
}
